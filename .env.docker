# Docker Compose — local host (copy values; do not commit secrets in production)

POSTGRES_USER=matrix
POSTGRES_PASSWORD=matrix_prod_change_me
POSTGRES_DB=matrix_prod

MARKETING_PORT=3000
PLATFORM_PORT=3001
GATEWAY_PORT=8080
NEXT_PUBLIC_SITE_URL=http://localhost:8080

ADMIN_EMAIL=admin@matrix.local
ADMIN_PASSWORD=matrix_admin_change_me
SESSION_SECRET=matrix_session_secret_change_me
