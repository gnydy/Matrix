import { NextResponse } from 'next/server';import { prisma } from '@allinall/database';export const dynamic='force-dynamic';export async function GET(){const started=Date.now();try{await prisma.$queryRaw`SELECT 1`;return NextResponse.json({status:'ok',service:'matrix-web-dashboard',database:'ok',release:process.env.MATRIX_RELEASE_SHA||'local',environment:process.env.MATRIX_RELEASE_ENV||process.env.NODE_ENV||'unknown',latencyMs:Date.now()-started,timestamp:new Date().toISOString()},{headers:{'cache-control':'no-store'}});}catch(error){console.error(error);return NextResponse.json({status:'error',service:'matrix-web-dashboard',database:'down',timestamp:new Date().toISOString()},{status:503,headers:{'cache-control':'no-store'}})}}
