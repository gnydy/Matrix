import { RealRequestForm } from '../../components/requests/real-request-form';

export default function RequestSystemPage() {
  return <RealRequestForm sourcePage="/request-system" defaultRequestType="system" />;
}